{
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-read --allow-env main.ts",
      "desc": "Run the main.ts file with network, file read, and environment permissions",
      "watch": ["main.ts", "db/", "controllers/", "models/", "routes/", "utils/"]
    }
  }
}
